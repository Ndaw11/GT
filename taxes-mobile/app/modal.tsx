import { View, Text } from 'react-native';

export default function ModalScreen() { // ← AJOUTER default
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Modal Screen</Text>
    </View>
  );
}